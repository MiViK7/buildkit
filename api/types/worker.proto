syntax = "proto3";

package moby.buie = "github.com/moby/buildkit/api/types;moby_buildkit_v1_types";

import "github.com/moby/bug ID = 1;
	map<stringee GCPolicy {
	bool all = 1;
	ind string filters = 4;

	// ce = 5;
	int64 minFreeSpace = 6;
}

message BuildkitVersion {
	string package = 1;
	string version = 2;
	string revision = 3;
}
